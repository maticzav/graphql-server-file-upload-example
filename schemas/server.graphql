# source: http://localhost:5000
# timestamp: Sat Dec 09 2017 10:26:24 GMT+0100 (CET)

scalar DateTime

type DeletePicturePayload {
  success: Boolean!
}

type Mutation {
  renamePicture(id: ID!, name: String!): Picture
  deletePicture(id: ID!): DeletePicturePayload!
}

interface Node {
  id: ID!
}

type Picture implements Node {
  contentType: String!
  createdAt: DateTime!
  id: ID!
  name: String!
  secret: String!
  size: Int!
  updatedAt: DateTime!
  url: String!
}

type Query {
  picture(id: ID!): Picture
  allPictures: [Picture!]!
}
